# Журнал реализации

## Назначение

Этот файл фиксирует ход разработки интерактивной визуализации курсовой
работы по ОТС. Проект реализуется последовательно, по одному смысловому
блоку тракта за раз.

Перед началом реализации каждого блока обязательно проводится отдельное
исследование методических указаний `coursework_methodical_instructions.docx`.
Нельзя добавлять расчёт или формулу только на основании общего понимания
темы: сначала нужно найти соответствующий раздел методички и зафиксировать
основание для реализации в этом журнале.

## Обязательный процесс работы над блоком

Для каждого блока нужно пройти все этапы:

1. Найти относящиеся к блоку разделы методических указаний.
2. Выписать, что физически происходит с сигналом.
3. Зафиксировать входной и выходной сигналы.
4. Перепроверить формулы по визуальному представлению в Word, если текстовое
   извлечение потеряло дроби, индексы или специальные символы.
5. Выбрать обязательный основной график преобразования сигнала.
6. Определить дополнительные поясняющие графики, если без них причина
   преобразования неочевидна.
7. Реализовать расчёт, описание и визуализацию.
8. Проверить численный результат и поведение интерфейса.
9. Записать итог и ограничения в этот журнал.

## Критерии готовности одного блока

Блок можно считать завершённым, только если:

- указаны использованные разделы методички;
- описан физический смысл преобразования;
- зафиксированы формулы и исходные параметры;
- присутствует минимум один график изменения сигнала;
- дополнительные графики добавлены там, где они помогают объяснению;
- графики обновляются при изменении доступных параметров;
- интерфейс показывает входной и выходной сигналы;
- выполнена ручная проверка страницы в браузере;
- журнал обновлён.

## Очередь блоков

| № | Блок | Вход | Выход | Статус |
| --- | --- | --- | --- | --- |
| 0 | Базовая структура приложения | — | оболочка интерфейса | выполнено |
| 1 | Исходные данные варианта | таблица вариантов | набор параметров | ожидает исследования |
| 2 | Источник и первичный преобразователь | `c(t)` | `g(t)` | ожидает исследования |
| 3 | Передающий ФНЧ | `g(t)` | `x(t)` | ожидает исследования |
| 4 | Дискретизатор АЦП | `x(t)` | `x(k · Δt)` | ожидает исследования |
| 5 | Квантователь АЦП | `x(k · Δt)` | `vₖʲ` | ожидает исследования |
| 6 | Кодер АЦП | `vₖʲ` | `bₖᵘ` | ожидает исследования |
| 7 | Модулятор и выход ПДУ | `bₖᵘ`, `uₙ(t)` | `S(t)` | ожидает исследования |
| 8 | Непрерывный канал связи | `S(t)`, `n(t)` | `z(t)` | ожидает исследования |
| 9 | Вход ПРУ, детектор и РУ | `z(t)` | `b̂ₖᵘ` | ожидает исследования |
| 10 | Декодер и интерполятор ЦАП | `b̂ₖᵘ` | `x̂(t)` | ожидает исследования |
| 11 | Приёмный ФНЧ и получатель | `x̂(t)` | `ĉ(t)` | ожидает исследования |
| 12 | Итоговая оценка восстановления | все виды ошибок | `δ²Σ` | ожидает исследования |

## Записи

### 2026-06-01 — Базовая структура приложения

Создан чистый каркас одностраничного приложения:

- `index.html` — семантическая оболочка;
- `styles.css` — базовая визуальная система и адаптивная раскладка;
- `app.js` — реестр будущих этапов, построение маршрута и выбор заглушек;
- `PROJECT_CONTEXT.md` — общий контекст и реконструкция исходной схемы.

Расчётные блоки, формулы и графики намеренно не реализованы. В каркасе
подготовлены места для последующего заполнения после исследования каждого
этапа.

## Шаблон записи следующего блока

```md
### YYYY-MM-DD — Название блока

**Разделы методички:** ...

**Что происходит с сигналом:** ...

**Вход:** ...

**Выход:** ...

**Формулы:** ...

**Основной график:** ...

**Дополнительные графики:** ...

**Реализация:** ...

**Проверка:** ...

**Ограничения и решения:** ...
```
